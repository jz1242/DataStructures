
/**Interface resetablecounter that takes from counter.
 * @author JZ
 *
 */
public interface ResetableCounter extends Counter {
    /**allow for the rest of a counter that implements this class.
     */
    void reset();

}
